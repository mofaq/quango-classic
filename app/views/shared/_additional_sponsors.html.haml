
-if current_group.has_sponsors?

  -if width == "wide"
    .block
      %h4
        = current_group.sponsors_label
      .content
        -if current_group.has_sponsors?
          = "Sponsors list"

  -if width == "narrow"
    .block
      %h4
        = current_group.sponsors_label
      .content

        -if current_group.has_sponsors?

          = "Sponsors list"




.clear



